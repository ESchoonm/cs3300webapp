<%= form_with(model: bookrake) do |form| %>
  <% if bookrake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookrake.errors.count, "error") %> prohibited this bookrake from being saved:</h2>

      <ul>
        <% bookrake.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :db %>
    <%= form.text_field :db %>
  </div>

  <div class="field">
    <%= form.label :Book %>
    <%= form.text_field :Book %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
