<p id="notice"><%= notice %></p>

<h1>Books</h1>

<section class="myborder">
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Genre</th>
      <th>Date released</th>
      <th>Book in series</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.description %></td>
        <td><%= book.genre %></td>
        <td><%= book.date_released %></td>
        <td><%= book.book_in_series %></td>
        <td><%= button_to 'Show', book %></td>
        <td><%= button_to 'Edit', edit_book_path(book) %></td>
        <td><%= button_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</section>
<br>

<%= button_to 'New Book', new_book_path %>
